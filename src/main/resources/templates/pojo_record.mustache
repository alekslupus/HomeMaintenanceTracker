package {{package}};

import io.soabase.recordbuilder.core.RecordBuilder;
{{#imports}}
    import {{import}};
{{/imports}}

/**
* {{description}}
*/
@RecordBuilder
public record {{classname}} (
{{#vars}}
    {{#isEnum}}
        {{datatype}} {{name}}{{#hasMore}},{{/hasMore}}{{^hasMore}}{{/hasMore}}
    {{/isEnum}}
    {{^isEnum}}
        {{datatype}} {{name}}{{#hasMore}},{{/hasMore}}{{^hasMore}}{{/hasMore}}
    {{/isEnum}}
{{/vars}}
) {
}
